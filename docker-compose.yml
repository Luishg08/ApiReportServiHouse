version: "3.9"

services:
  apireport:
    build: .
    container_name: apireport-container
    ports:
      - "8088:8088"
    networks:
      - gateway-network

networks:
  gateway-network:
    external: true